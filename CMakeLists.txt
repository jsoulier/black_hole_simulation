cmake_minimum_required(VERSION 3.24)
project(savepoint)

add_library(savepoint_sqlite3 src/sqlite3.c)
add_library(savepoint src/savepoint.cpp)
add_library(savepoint::savepoint ALIAS savepoint)
target_link_libraries(savepoint PRIVATE savepoint_sqlite3)
target_include_directories(savepoint PUBLIC include)
set_target_properties(savepoint PROPERTIES CXX_STANDARD 23)
set_target_properties(savepoint PROPERTIES WINDOWS_EXPORT_ALL_SYMBOLS ON)
set_target_properties(savepoint_sqlite3 PROPERTIES WINDOWS_EXPORT_ALL_SYMBOLS ON)
target_precompile_headers(savepoint PUBLIC <savepoint.hpp>)

if(PROJECT_IS_TOP_LEVEL)
    set(BINARY_DIR ${CMAKE_BINARY_DIR}/bin)
    set(CMAKE_RUNTIME_OUTPUT_DIRECTORY ${BINARY_DIR})
    set(CMAKE_RUNTIME_OUTPUT_DIRECTORY_RELEASE ${BINARY_DIR})
    set(CMAKE_RUNTIME_OUTPUT_DIRECTORY_DEBUG ${BINARY_DIR})
    set(CMAKE_LIBRARY_OUTPUT_DIRECTORY ${BINARY_DIR})
    set(CMAKE_LIBRARY_OUTPUT_DIRECTORY_RELEASE ${BINARY_DIR})
    set(CMAKE_LIBRARY_OUTPUT_DIRECTORY_DEBUG ${BINARY_DIR})
    make_directory(${BINARY_DIR})
    add_executable(savepoint_test test/savepoint.cpp)
    set_target_properties(savepoint_test PROPERTIES CXX_STANDARD 23)
    target_link_libraries(savepoint_test savepoint)
    add_test(NAME savepoint_test COMMAND savepoint_test)
endif()